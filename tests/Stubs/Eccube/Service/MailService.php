<?php

namespace Eccube\Service;

/**
 * Minimal stub of Eccube\Service\MailService so MailExService (which extends
 * it) can be mocked in unit tests without a full EC-CUBE install.
 */
class MailService
{
    public function __construct()
    {
    }
}
